/**
 * Members and their devices: member, member_device, member_program.
 *
 * <p>One phone number = one member within an org — {@code UNIQUE (org_id, phone_normalized)}, not
 * globally unique (D8).
 */
package com.checkino.member;
